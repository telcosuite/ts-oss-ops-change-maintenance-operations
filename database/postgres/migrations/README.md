# App-Owned Migrations

Add Flyway migrations for `change_maintenance` here.

The copied SQL files under `../source-migrations/` are source artifacts from the planning repository. During implementation, split the suite starter DDL and TMF refinement SQL into app-owned migrations that create only this app schema and its write/read models.
