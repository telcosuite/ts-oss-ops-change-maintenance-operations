# Event Contracts

Replace the bootstrap schema with versioned event contracts discovered from the app dev tasks. Event contracts must include correlation, causation, tenant, replay, compatibility, and outbox behavior.
