# API And Event Design

Use `contracts/openapi/v1/openapi.yaml` and `contracts/events/` as implementation contract locations. Replace placeholders with TMF-aligned app APIs and event schemas from the dev tasks.
